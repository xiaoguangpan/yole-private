# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
build/
dist/

# Virtual environments
.venv/
venv/
env/
ENV/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo

# Node (for desktop/)
node_modules/

# Tauri / desktop build artifacts
desktop/dist/
desktop/src-tauri/target/
desktop/src-tauri/gen/
# Bundled Python distributions (regenerated by scripts/bundle-python.sh,
# ~80MB per arch — never committed).
desktop/src-tauri/python-bundle/
# PBS tarball download cache so we don't refetch on every bundle run.
.cache/pbs/

# Local
.env
.env.local
*.log

# Design handoff bundle: keep prototype, drop the uploads/ duplicates
# (those are CLAUDE.md / PRD.md / DESIGN.md copies the design agent
# received as input; the canonical files live elsewhere in docs/).
docs/GenericAgent Workbench-handoff/project/uploads/
