# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
build/
dist/

# Virtual environments
.venv/
venv/
env/
ENV/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo

# Node (for gui/)
node_modules/
.pnpm-store/

# Tauri / GUI build artifacts
gui/dist/
core/target/
core/gen/
core/tauri.updater.generated.conf.json
core/experiments/tray-mode/target/
core/experiments/tray-mode/gen/
core/experiments/tray-mode/Cargo.lock
cli/target/
# Bundled Python distributions (regenerated by scripts/bundle-python.sh,
# ~80MB per arch — never committed).
core/python-bundle/
# PBS tarball download cache so we don't refetch on every bundle run.
.cache/pbs/

# Local
.env
.env.local
*.log
.local/
plan.md
release-package/
provisioner/bin/

# Design handoff bundle: keep prototype, drop the uploads/ duplicates
# (those are AGENTS.md / PRD.md / DESIGN.md copies the design agent
# received as input; the canonical files live elsewhere in docs/).
docs/design-handoff/project/uploads/
